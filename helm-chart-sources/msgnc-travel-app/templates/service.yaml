apiVersion: v1
kind: Service
metadata:
  name: {{ include "msgnc-travel-app.fullname" . }}
  labels:
    app: {{ include "msgnc-travel-app.name" . }}
    chart: {{ include "msgnc-travel-app.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ include "msgnc-travel-app.name" . }}
    release: {{ .Release.Name }}
